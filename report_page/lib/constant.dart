import 'package:flutter/material.dart';

// === Colors ===
const kWhite = Colors.white;
const kLightGrey = Color(0xFFF7F5F7);   // 🔑 one grey used for both AppBar & Card
const kDividerGrey = Color(0xFFE5E6EA);

// === Radii ===
const kCardRadius = 16.0;

// === Padding ===
const kTileHPad = 16.0;
const kTileVPad = 14.0;

// === Durations ===
const kSnackDuration = Duration(milliseconds: 900);
